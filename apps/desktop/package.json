{
  "name": "m_assistant-desktop",
  "version": "0.0.0",
  "private": true,
  "main": "main.cjs",
  "scripts": {
    "build:web": "npm --prefix ../web run build",
    "start": "electron .",
    "dev": "concurrently -k -n WEB,ELECTRON -c cyan,magenta \"npm --prefix ../web run dev -- --host 127.0.0.1 --port 5173\" \"wait-on http://127.0.0.1:5173 && set DESKTOP_DEV_URL=http://127.0.0.1:5173 && electron .\""
  },
  "dependencies": {
    "electron": "^31.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "wait-on": "^7.2.0"
  }
}
